#view the first few rows of mtcars dataset
head(mtcars)

#calculate the average mpg among all cars and save it as a variable
mean_mpg <- mean(mtcars$mpg)

#print out the mean mpg
mean_mpg

#calculate the sum mpg and save it as a variable
sum_mpg <- sum(mtcars$mpg)

#print out the sum mpg
sum_mpg
